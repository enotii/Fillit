void ft_printmap(t_f)
size_t	i;

	i = 0;
	g_map = (char **)malloc(sizeof(char *) * (size + 1));
	while (i < size)
	{
		g_map[i] = ft_strnew(size);
		ft_memset((void*)g_map[i], '.', size);
		i++;
	}
	g_map[i] = NULL;